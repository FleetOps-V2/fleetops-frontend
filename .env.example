# FleetOps Frontend — Environment Variables
# Copy to .env for local development (Vite reads VITE_* vars at build time)

# API base URL — set to "/" so Vite's dev proxy intercepts /api/* requests.
# In production (Docker/K8s), nginx proxies /api/* to backend services directly.
VITE_API_BASE_URL=/
