# API base URL — set to '/' when running behind Nginx or the Vite dev proxy.
# For production on AWS, set this to your CloudFront/ALB domain e.g.:
# VITE_API_BASE_URL=https://api.fleetops.example.com
VITE_API_BASE_URL=/
